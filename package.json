{
  "name": "llm-war",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/main.ts",
    "start": "bun src/main.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "build": "bun build --compile --minify src/main.ts --outfile dist/llmwar"
  },
  "dependencies": {
    "@jitl/quickjs-singlefile-cjs-release-sync": "^0.31.0",
    "pixi.js": "^8.6.6",
    "quickjs-emscripten-core": "^0.31.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.7.0"
  }
}
